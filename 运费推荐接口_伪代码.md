# 运费智能柜型推荐 — 后端接口伪代码

> 对应原型 v1.8「⑥ 报价单生成」的【智能柜型推荐】面板。
> 用途：把"输入货量 CBM + 重量 KG → 自动对比 散货/20GP/40HQ 三档并高亮最省"的逻辑，从前端 JS 抽象成后端可实现的接口。
> 约定：海运段以 USD 计、国内段以 CNY 计；比较时统一折算为 USD。

---

## 1. 接口定义

```
POST /api/quotation/recommend
```

| 项 | 说明 |
|----|------|
| 鉴权 | 复用 ERP 现有会话鉴权（原型中未做） |
| 幂等 | 纯查询，无副作用 |
| 依赖表 | `freight_rate_detail`（海运段）· `factory_freight_rate`（国内段）· `factory`（厂址主数据）· `config_container`（柜型主数据，含 volume/max_weight）· 配置项（`default_domestic` / `fx_rate` / `lcl_breakeven_cbm`） |

### 1.1 请求体（Request）

```json
{
  "factory_code": "F001",        // 起运厂址（必填，决定国内段）
  "origin_port":  "深圳(盐田)",   // 起运港（必填）
  "dest_port":    "曼萨尼约",     // 目的港（必填）
  "carrier":      "恒荣达",       // 船公司（必填）
  "cbm":          12,             // 货量体积，CBM（必填，>0）
  "kg":           8000,           // 货量毛重，KG（可选，>0 时做载重校验）
  "include_lcl":  true,           // 是否纳入散货 LCL 对比（默认 true）
  "fx_rate":      7.2             // USD→CNY 折算汇率（可选，缺省读配置）
}
```

### 1.2 响应体（Response）

```json
{
  "recommended": "LCL(散货拼箱)",
  "breakeven_cbm": 15,
  "candidates": [
    {
      "container_type": "LCL(散货拼箱)",
      "sea_usd": 8357,
      "domestic_cny": 1020,
      "total_usd": 8499,
      "recommended": true,
      "is_demo": true,              // 散货海运价为示例推导（无真实 LCL 价）
      "is_fallback": false,
      "over_weight": false,
      "note": "示例海运价（按 20GP 总价÷15CBM 推导）"
    },
    {
      "container_type": "20GP",
      "sea_usd": 10263,
      "domestic_cny": 950,
      "total_usd": 10494,
      "recommended": false,
      "is_demo": false,
      "is_fallback": false,
      "over_weight": false,
      "note": ""
    },
    {
      "container_type": "40HQ",
      "sea_usd": 12671,
      "domestic_cny": 1300,
      "total_usd": 14377,
      "recommended": false,
      "is_demo": false,
      "is_fallback": false,
      "over_weight": false,
      "note": ""
    }
  ],
  "warnings": [
    "散货(LCL)海运价为示例推导，盈亏平衡点约 15 CBM；录入真实散货海运价后结论会更准"
  ]
}
```

---

## 2. 依赖的配置项（建议放 `config_setting` 表）

| key | 含义 | 缺省 |
|-----|------|------|
| `fx_rate` | USD→CNY 折算汇率 | 7.2 |
| `lcl_breakeven_cbm` | 散货示例推导用的盈亏平衡点 | 15 |
| `default_domestic` | 国内段默认档（缺失时回退） | `{"factory":"F001","origin":"深圳(盐田)","ct":"20GP"}` |

> 默认档用途：演示/试算时，若某厂址×起运港×柜型无国内段报价，自动套用此基准价，避免前端空白。真实环境应要求业务补全，而非长期依赖默认档。

---

## 3. 核心算法伪代码（Python）

```python
# ---------- 全局/配置 ----------
FX_RATE          = 7.2      # 可配置
LCL_BREAKEVEN    = 15       # 散货示例推导盈亏平衡点(CBM)，可配置
DEFAULT_DOMESTIC = {"factory": "F001", "origin": "深圳(盐田)", "ct": "20GP"}
PENDING_ORIGIN   = "（待补充）"   # 历史数据起运港占位

# ---------- 入口 ----------
def recommend_quote(factory_code, origin_port, dest_port, carrier,
                    cbm, kg=0, include_lcl=True, fx_rate=FX_RATE):
    if cbm <= 0:
        raise BadRequest("cbm 必须大于 0")

    candidates = []
    # 整柜档：20GP + 40HQ
    for ct in ("20GP", "40HQ"):
        sea_usd, sea_demo, _ = sea_freight(origin_port, dest_port, carrier, ct)
        dom_cny, dom_fb      = domestic_freight(factory_code, origin_port, ct)
        candidates.append(_build(sea_usd, dom_cny, ct, sea_demo, dom_fb, fx_rate))

    # 散货档：依赖真实 LCL 海运价；缺失则示例推导
    if include_lcl:
        candidates.append(lcl_candidate(factory_code, origin_port, dest_port,
                                        carrier, cbm, fx_rate))

    # 统一折算为 USD 并比较
    for c in candidates:
        c["total_usd"] = c["sea_usd"] + c["domestic_cny"] / fx_rate
    best = min(candidates, key=lambda x: x["total_usd"])
    for c in candidates:
        c["recommended"] = (c is best)
        mw = container_max_weight(c["container_type"])
        c["over_weight"] = (kg > 0 and mw and kg > mw)

    warnings = []
    if include_lcl and any(c["container_type"].startswith("LCL") and c["is_demo"] for c in candidates):
        warnings.append(f"散货(LCL)海运价为示例推导，盈亏平衡点约 {LCL_BREAKEVEN} CBM；"
                        "录入真实散货海运价后结论会更准")
    return {
        "recommended": best["container_type"],
        "breakeven_cbm": LCL_BREAKEVEN if include_lcl else None,
        "candidates": candidates,
        "warnings": warnings,
    }

# ---------- 海运段（含默认档回退） ----------
def sea_freight(origin, dest, carrier, ct):
    """返回 (金额USD, 是否示例推导, 回退柜型)。规则与估价表一致。"""
    rows = SELECT * FROM freight_rate_detail
           WHERE dest_port = dest AND carrier = carrier
             AND container_type = ct
             AND origin_port IN (origin, PENDING_ORIGIN)   # 软匹配：真实港 + 待补充
             AND effective_from <= today <= effective_to
    if rows:
        return sum(r.amount for r in rows), False, None
    # 回退 1：同航线 20GP
    fb = SELECT * FROM freight_rate_detail
         WHERE dest_port=dest AND carrier=carrier AND container_type='20GP'
           AND origin_port IN (origin, PENDING_ORIGIN) AND valid
    if fb:
        return sum(r.amount for r in fb), True, "20GP"
    # 回退 2：同航线任意柜型
    fb2 = SELECT * FROM freight_rate_detail
          WHERE dest_port=dest AND carrier=carrier
            AND origin_port IN (origin, PENDING_ORIGIN) AND valid
    if fb2:
        return sum(r.amount for r in fb2), True, fb2[0].container_type
    return 0.0, True, None

# ---------- 国内段（含默认档回退） ----------
def domestic_freight(factory_code, origin_port, ct):
    """返回 (金额CNY, 是否默认档回退)。"""
    rows = SELECT * FROM factory_freight_rate
           WHERE factory_code = factory_code AND origin_port = origin_port
             AND container_type = ct AND valid
    if rows:
        return sum(r.amount for r in rows), False
    # 默认档回退
    if DEFAULT_DOMESTIC:
        d = SELECT * FROM factory_freight_rate
            WHERE factory_code = DEFAULT_DOMESTIC["factory"]
              AND origin_port  = DEFAULT_DOMESTIC["origin"]
              AND container_type = DEFAULT_DOMESTIC["ct"] AND valid
        if d:
            return sum(r.amount for r in d), True
    return 0.0, True

# ---------- 散货 LCL 候选（核心：缺失价 → 示例推导） ----------
def lcl_candidate(factory_code, origin_port, dest_port, carrier, cbm, fx_rate):
    # 海运段
    lcl_rows = SELECT * FROM freight_rate_detail
               WHERE dest_port=dest_port AND carrier=carrier
                 AND container_type='LCL(散货拼箱)'
                 AND origin_port IN (origin_port, PENDING_ORIGIN) AND valid
    if lcl_rows:
        sea_rate = sum(r.amount for r in lcl_rows)   # 已按CBM计的总额
        sea_demo = False
    else:
        # 示例推导：20GP 海运总价 ÷ 盈亏平衡点
        sea20, _, _ = sea_freight(origin_port, dest_port, carrier, "20GP")
        sea_rate = sea20 / LCL_BREAKEVEN
        sea_demo = True
    sea_usd = sea_rate * cbm

    # 国内段（散货按 CBM）
    dom_rows = SELECT * FROM factory_freight_rate
               WHERE factory_code=factory_code AND origin_port=origin_port
                 AND container_type LIKE '%LCL%' AND valid
    if dom_rows:
        dom_rate = sum(r.amount for r in dom_rows)   # 按CBM总额
        dom_fb = False
    else:
        dom20, _ = domestic_freight(factory_code, origin_port, "20GP")
        dom_rate = dom20 / 33.2                       # 20GP 容积(CBM)
        dom_fb = True
    dom_cny = dom_rate * cbm
    return _build(sea_usd, dom_cny, "LCL(散货拼箱)", sea_demo, dom_fb, fx_rate)

# ---------- 工具 ----------
def _build(sea_usd, dom_cny, ct, sea_demo, dom_fb, fx_rate):
    return {
        "container_type": ct, "sea_usd": round(sea_usd),
        "domestic_cny": round(dom_cny),
        "total_usd": round(sea_usd + dom_cny / fx_rate),
        "recommended": False, "is_demo": sea_demo,
        "is_fallback": dom_fb, "over_weight": False,
        "note": ("示例海运价（按 20GP 总价÷%dCBM 推导）" % LCL_BREAKEVEN) if sea_demo else "",
    }

def container_max_weight(ct):
    row = SELECT max_weight FROM config_container WHERE code = ct
    return row.max_weight if row else None   # LCL 为 NULL
```

---

## 4. 与「普通报价单」的关系（复用而非重复）

推荐接口是**普通估价接口 `GET /api/quotation` 之上的薄层**：

```
GET  /api/quotation          → 给定 (factory,origin,dest,carrier,ct) 聚合 海运段+国内段
POST /api/quotation/recommend → 对 ct∈{20GP,40HQ,LCL} 各调一次估价，再横向比较取最省
```

普通估价聚合 SQL（供推荐接口内部复用）：

```sql
-- 海运段
SELECT category, fee_code, SUM(amount) AS amt
FROM freight_rate_detail
WHERE dest_port=:dest AND carrier=:carrier AND container_type=:ct
  AND origin_port IN (:origin, '（待补充）')
  AND effective_from <= :today AND effective_to >= :today
GROUP BY category, fee_code;

-- 国内段
SELECT SUM(amount) AS dom_cny
FROM factory_freight_rate
WHERE factory_code=:factory AND origin_port=:origin AND container_type=:ct
  AND effective_from <= :today AND effective_to >= :today;
```

前端"选用此柜型"= 拿 `recommended` 回填到估价页的 `container_type`，再调 `GET /api/quotation`。

---

## 5. 关键业务规则（与行业/原型一致）

1. **起运港软匹配**：`origin_port IN (真实港, '（待补充）')` —— 兼容历史数据未注明起运港的情形。
2. **海运段回退链**：精确柜型 → 20GP → 同航线任意柜型；回退行标 `is_demo=true`（"默认档"）。
3. **国内段回退**：精确 → 默认档(`default_domestic`)；回退行标 `is_fallback=true`。
4. **散货示例推导**：仅当无真实 LCL 海运价时，用 `20GP海运总价 ÷ lcl_breakeven_cbm` 估算每 CBM 单价；响应 `is_demo=true` **且** `warnings` 提示，绝不伪装成真实价；前端"含散货对比"开关关闭时整栏不出现。
5. **重量校验**：`kg > config_container.max_weight` 时 `over_weight=true`（LCL 无 max_weight，跳过）。
6. **币种归一**：国内段 CNY ÷ `fx_rate` 折算 USD 后比较；汇率应取**计费日汇率**，原型固定 7.2 仅为演示。
7. **盈亏平衡点 ≈ 15 CBM**：低于约 15 CBM 散货更省，高于则 20GP 整柜更优（验证：12 CBM→LCL，20/30 CBM→20GP）。

---

## 6. 上线前待补（开发对接清单）

- [ ] 真实散货(LCL)海运价按航线录入（`freight_rate_detail`，柜型=`LCL(散货拼箱)`，计费方式=按CBM），录入后 `is_demo` 自动消失。
- [ ] `fx_rate` 改为按计费日取汇率表，而非固定常量。
- [ ] `lcl_breakeven_cbm`、柜型 volumetric/weight 阈值应**按航线可配置**，比全国统一的 15 更准。
- [ ] 历史 68 条起运港由"（待补充）"补全为真实港口，软匹配即可退场。
- [ ] 鉴权、限流、审计（谁在何时试算过报价）。
