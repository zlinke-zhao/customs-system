# PRD｜出口装箱单 & 送货单 子模块（运费项目明细设定）

> 文档版本：v0.1（评审稿）　|　作者：产品　|　日期：2026-09-04　|　归属模块：运费项目明细设定
> 适用范围：海运出口业务，装箱单 / 送货单 / 派车单 三单联动设计

---

## 1. 问题陈述（Problem Statement）

在把"运费项目明细设定"模块拆子项时，出口装箱单与送货单的关系出现两个反复踩坑的设计疑问：

- **次序不清**：两份单都必不可少，但业务上谁先谁后不固定（工厂自送常"送货单先→装柜→装箱单"；先预报报关则"装箱单先→拉货再送货单"；外包货代则工厂只出送货单）。若强设先后依赖，部分客户流程直接走不通。
- **粒度冲突**：送货单需按"料号 × 客户PO"分行（客户要按 PO 追溯），装箱单却需按"料号"合并（报关/装柜只看物理包装）。同时一个柜常拼多家委托方客户的货，原设计缺"客户"维度，导致运费分摊、分账无法落地。

代价：不解决则子项拆分后各单数据对不齐、运费算不清、报关装箱单与内部账目脱节，上线即返工。

---

## 2. 目标（Goals）

| # | 目标 | 如何衡量成功 |
|---|---|---|
| G1 | 三单（出货通知单/送货单/装箱单/派车单）数据可对齐、可互查 | 同一出货通知单号下，任意两单能自动匹配关联 |
| G2 | 装箱单支持多合并维度，同时满足报关与内部分账 | 同一数据源可一键生成"按料号主单"与"按客户分单" |
| G3 | 支持一柜多委托方客户，运费可按客户体积/重量分摊 | 一个柜的运费能拆分到各客户且无遗漏/重复 |
| G4 | 不强制单序，适应 A/B/C 三类业务流 | 三种流程均可完整走通，无需改数据结构 |

---

## 3. 非目标（Non-Goals）

| # | 不做什么 | 原因 |
|---|---|---|
| NG1 | 不做越南进口报关单对接 | 已单列二期，避免领导误解（见前期结论） |
| NG2 | 不内置报关申报/舱单直传海关 | 属报关系统职责，本模块只出单 |
| NG3 | 不做多语言箱单（仅中文+必要英文品名） | 当前客户群不需要，后续可加 |
| NG4 | 不绑定特定 ERP 的库存接口 | 保持数据源独立，先接本系统销售订单/委托单 |

---

## 4. 用户故事（User Stories）

- 作为**操作员**，我希望在创建装箱单/送货单时都能选择同一个"出货通知单号"，以便三单自动关联、互相对账。
- 作为**操作员**，我希望装箱单能按"料号"合并出主单给报关、又能按"客户"出分单给财务，以便一套数据两处用。
- 作为**财务**，我希望一个柜的运费能按客户的体积/重量分摊到各委托方，以便向每家客户独立开票。
- 作为**跟单**，我希望送货单按"料号 × 客户PO"分行、装箱单保留 PO 子明细，以便客户按 PO 对账、分票报关。
- 作为**管理者**，我希望无论"先送货单后装箱单"还是"先装箱单后送货单"流程都能跑，以便适配不同客户习惯。

---

## 5. 需求（Requirements）

### P0 必须（MVP）

**R1｜父实体"出货通知单/发运单" + 柜子实体**
- 出货通知单（Shipment）为唯一锚点，承载：出货通知单号(PK)、起运港、目的港、船名航次、预计开船日、状态。**创建者=发货人侧跟单/业务/单证岗（或从 ERP 销售订单自动生成）；创建节点=销售订单确认、安排发货之后，且早于三单与订舱/报关（见 §5.5）。**
- **柜（Container）为 Shipment 子实体**：柜号(PK)、封条号、箱数、毛重、体积、提单号；一委托可含多柜（一票多柜）。
- 送货单、装箱单、派车单 均含 `出货通知单号(FK)` 关联父实体；装箱单另含 `柜号(FK)`。

**R2｜软关联、无强制先后**
- 装箱单"关联送货单号"字段**可选**，在"确认/封板"时填写；系统优先用共同 `出货通知单号/柜号` 自动匹配（一个柜可对应多张送货单/装箱单）。
- 不得把任一单设为另一单的创建前置条件。

**R3｜单一数据源粒度 = 客户 × 料号 × PO × 柜**
- 送货单明细行、出货通知单明细行以 `客户 × 料号 × PO × 柜` 为最细粒度（客户、柜均属明细维度，非仅表头）。
- 装箱单为该数据源的聚合视图（按柜/总箱单/按客户/按PO），不另存物理明细。

**R4｜装箱单合并维度（至少三种）**
- 支持"按柜（每柜一张，基准）"、"总箱单（跨柜汇总，报关/总账）"、"按客户（分单/分账）"。
- 合并键 = `料号`（同料号须箱规一致；箱规不同则 `料号+箱规`）。

**R5｜多客户（一柜多委托方）**
- 一个出货通知单可挂多个委托方客户；主装箱单对外（GROUP BY 料号），分箱单×客户对内（GROUP BY 客户）。

### P1 应做（上线后快跟）

- **R6｜PO 子明细**：合并后的装箱单行可展开显示 `PO号 / 数量 / 箱数 / 占比`，满足追溯与分票报关。
- **R7｜合并维度扩展**：增加"按料号+PO""按PO"两种视图。
- **R8｜运费分摊**：由「费用汇总与分摊」模块（§12）统一实现；按出货通知单归集全部海运费用后，经送货单明细（客户×料号×PO×体积/重量）分摊到各客户/PO/料号，默认权重=体积优先，可切换重量/货值。

### P2 未来（架构预留）

- **R9｜分票报关**：一个柜按客户/PO 拆多票报关时，装箱单自动对应多份报关资料。
- **R10｜LCL 主单/分单（house B/L）**：拼箱场景主单一个、分单多个。

---

## 5.5 出货通知单（Shipment）：流程起点与创建规则

> 本模型的根实体。此前为解开"三单次序不清"把它作为共同锚点引入，但它本身从何而来必须讲清，否则下游单据的出货通知单号成了无源之水。

**是什么**：出货通知单（又称发运单 / 托单 / 订舱委托，英文实体名 Shipment）是"**我要发这一票货**"的业务起点单据，代表一笔待发运的出口业务。下游装箱单、送货单、派车单、费用、报关全部挂在它下面。

**谁开**：发货人（卖方）侧的 **跟单 / 业务 / 单证岗**；或由 **ERP 销售出货模块**从已确认销售订单自动生成。

**何时开 / 哪个节点**：**销售订单确认、决定安排发货之后**，且 **早于** 装箱单、送货单、派车单、订舱（SO）、报关。它是整条出口流程的**最上游**，必须先存在。

```
销售订单（客户要买什么）
   ↓ 确认、安排发货
【出货通知单 Shipment】→ 系统生成 出货通知单号   ← ★ 本节点创建，流程起点
   ↓ 之后才能开
   ├─ 出口装箱单（预报/实际）   出货通知单号(FK)
   ├─ 送货单（仓库备货）       出货通知单号(FK)
   ├─ 派车单（拖车拉柜）       出货通知单号(FK)
   └─ 费用汇总与分摊           出货通知单号(FK)
```

**与销售订单的关系**：销售订单 = 客户买了什么；出货通知单 = 我要发这票货。一张销售订单可拆多票出货通知单（分批发运），多张销售订单也可合并成一票出货通知单（拼货 / consolidation）。

**创建方式**：手动新建，或从销售订单"生成出货通知单"；创建时系统自动生成 `出货通知单号(PK)`，并填起运港、目的港、预计开船日、客户、运输条款、成交方式（默认=运输条款）、计划柜数、状态（草稿）。

**关键约束**：下游单据的 `出货通知单号` 是**引用（FK）已存在的 Shipment**，本模块不生成该号；未建出货通知单则下游单据开不了（出货通知单号必填）。

---

## 6. 数据模型

### 6.1 实体关系

```
出货通知单/发运单 (Shipment)            ← 父实体，唯一锚点；【流程起点，最先创建，见 §5.5】（可含多个柜）
 ├─ 柜 (Container)     出货通知单号(FK)   ← 柜号/封条号/箱数/体积/重量/提单号，装箱单承载单元
 ├─ 送货单 (DO)        出货通知单号(FK)   ← 仓库放行·拖车依据，明细 客户×料号×PO×柜
 ├─ 装箱单 (PL)        柜号(FK)         ← 每柜一张；报关·装船；总箱单为其聚合视图
 └─ 派车单 (Dispatch)  出货通知单号(FK)   ← 关联柜/装箱单号带出承运体积/重量（已有设计）
```

### 6.2 粒度与视图映射

| 层级 | 粒度 | 用途 |
|---|---|---|
| 单一数据源 | 客户 × 料号 × PO × 柜 | 送货单/出货通知单明细（最细，每箱归属某柜） |
| 装箱单·按柜 | GROUP BY 料号（限定单柜） | 每柜一张，封条核验/收货人拆柜（对外基准） |
| 装箱单·总箱单 | GROUP BY 料号（跨柜汇总） | 整票报关/总账（对外汇总） |
| 装箱单·分单 | GROUP BY 客户 → 料号 | 分账 / 运费分摊（对内） |
| 装箱单·追溯 | GROUP BY 料号+PO 或 PO | 内部核对（P1） |

---

### 6.3 多集装箱场景（一票多柜）

- 同一出货通知单下挂多个 柜（Container 子实体各一条，如 3 个柜）。
- 基准做法：**每柜开一张装箱单**（柜号+封条号表头），满足海关验封、收货人逐柜拆货。
- 系统一键生成**总箱单**（跨全部柜 GROUP BY 料号 汇总），用于整票报关与总账。
- 业务变体由"合并维度"开关覆盖，无需改结构：
  - 3 柜同属一票、客户只收一份 → 仅出总箱单；
  - 3 柜分属不同提单/收货人 → 各自独立装箱单、无总箱单。

### 6.4 运输条款 vs 成交方式（报关级贸易术语）

| 维度 | 主表「运输条款」 | 装箱单「成交方式」 |
|---|---|---|
| 层级 | 合同级（出货通知单/Shipment 表头） | 报关级（装箱单**表头/分单**，整单一个） |
| 来源 | 销售合同 / 订单 | 默认 = 主表运输条款；多客户分单可按客户不同 |
| 用途 | 决定海运费由买方还是卖方承担（运费估价开关） | 海关申报、审价、统计 |
| 典型值 | FOB、CIF、CFR、EXW 等 | FOB、CFR(C&F)、CIF 为主（见 §7.1） |
| 是否可一单多值 | 否，整票一个 | 主单统一；仅按客户出分单时各分单可不同 |

> 实务中**同一装箱单（同一票货）成交方式通常统一**，明细行直接继承表头即可，不必逐料号填。只有"一柜拼多家客户、各客户独立合同"时才按客户出分单、各分单成交方式不同。运费估价仍看主表运输条款。

## 7. 字段清单（草案）

### 7.0 柜（Container，Shipment 子实体）
| 字段 | 说明 | 备注 |
|---|---|---|
| 柜号 | PK（出货通知单内唯一） | 自动/手填 |
| 封条号 | 物理封条 | 装柜后生成 |
| 箱数 / 毛重 / 体积 | 该柜汇总 | 自动算 |
| 提单号 | 对应 B/L | 一柜一提单或一票多柜 |

### 7.1 出口装箱单（PL）

| 字段 | 说明 | 备注 |
|---|---|---|
| 装箱单号 | PK | 自动生成 |
| 出货通知单号 | FK → Shipment | 软关联键 |
| 柜号 | FK → Container | 引用柜子实体（柜号/封条号由其承载） |
| 运输条款 | 引用 Shipment（只读） | 合同级，表头打印，与发票/提单一致 |
| 成交方式 | 默认 = 运输条款（只读）；多客户分单可改 | 报关级，整单一个；用于海关申报 |
| 合并维度 | 按柜 / 总箱单 / 按客户 / 按料号+PO / 按PO | 生成时选择 |
| 单类 | 按柜箱单 / 总箱单 / 分单(客户A…) | 多柜出按柜箱单+总箱单；多客户出分单（各分单成交方式可不同） |
| 总件数 / 总毛重 / 总体积 | 汇总 | 自动算 |
| 明细行 | 料号、品名、箱数、毛重、净重、体积、PO子明细 | 合并后行可展开 PO；成交方式继承表头 |
| 报关字段组 | HS编码、单价、币种、原产国、境内货源地 | 海关申报用；成交方式在表头，不逐行填 |

### 7.2 送货单（DO）

| 字段 | 说明 | 备注 |
|---|---|---|
| 送货单号 | PK | 自动生成 |
| 出货通知单号 | FK → Shipment | 软关联键 |
| 发货厂址 / 起运港 | 物流执行信息 | 必填 |
| 委托方客户 | 明细行归属 | 一行一客户 |
| 明细行 | 客户 × 料号 × PO、应发/实发数量、箱数、毛重、体积 | 同料号按 PO 分行 |

---

## 8. 合并维度规则（核心逻辑）

1. 取该 `出货通知单号` 下全部 `客户 × 料号 × PO × 柜` 明细。
2. **按柜（基准）**：限定单柜号，组内按料号合并，生成"每柜一张"装箱单；柜号+封条号为表头，供海关验封与收货人拆柜。
3. **总箱单**：跨全部柜号 GROUP BY 料号 汇总，输出整票总箱单（报关/总账用）。
4. **按客户**：先按委托方客户分组，组内再按料号合并，输出"分箱单×客户"（分账/运费分摊）。
5. **按料号+PO / 按PO**：保留 PO 级行（追溯/分票用）。
6. 同料号箱规不同 → 不参与合并，各自成行并标注箱规。

---

## 9. 状态机（建议）

```
草稿 ──确认/封板──▶ 已确认 ──报关──▶ 已报关 ──装船──▶ 已发运
  │                  │
  └──── 作废 ────────┘
```
- "关联送货单号"在**已确认**节点填写（软匹配）。
- 已确认后不允许改合并维度主档，需废版重出。

---

## 10. 验收标准（节选 AC）

- [ ] Given 一个出货通知单号，When 创建装箱单并选"按料号"，Then 同料号多 PO 合并为一行且 PO 子明细可见。
- [ ] Given 一柜含客户 A、B、C 的货，When 选"按客户"生成，Then 输出三张分箱单且各客户体积/重量合计 = 主单总计。
- [ ] Given 先建送货单（未建装箱单），When 随后建装箱单并填同一出货通知单号，Then 系统自动关联，无强制先后拦截。
- [ ] Given 同料号但箱规不同，When 合并，Then 不强行合并，分别成行。
- [ ] Given 已确认装箱单，When 改合并维度，Then 被拒，须作废重出。

---

## 11. 成功指标（建议）

| 指标 | 目标 | 评估时点 |
|---|---|---|
| 三单关联成功率 | ≥98%（同委托号可互查） | 上线 1 月 |
| 装箱单生成耗时 | 较手工 < 5 分钟/柜 | 上线 1 月 |
| 运费分摊差错率 | 0（按体积/重量自动分摊） | 上线 1 季 |
| 多客户柜占比 | 可统计（基线先采集） | 持续 |

---

## 12. 费用汇总与分摊（实际结算）模块

> 与「⑥ 报价单生成」是两套：报价单 = 事前估价（**卖价 = 成本费率表(买价) + 按费用分类 markup**）；本模块 = 事后实际结算（**引用**物流对账单 + 海运对账单两张成本单据，经送货单分摊到订单/客户PO/料号）。

### 12.0 核心原则：三层分离、单一数据源（防重复）
- **成本费率表（买价）** 是唯一数据源：记录向车队/货代/码头实际支付的成本（即原「运费明细表」重定位为买价，不另建成本表）。
- **报价单（卖价）= 成本费率表(买价) 中每个费用项目自身的管理费率逐行计算**：卖价 = 买价 ×(1 + 管理费率)。管理费率落在<b>每个单价行</b>（成本费率表 + 国内段运费），「④ 默认管理费率」按费用分类预设默认值、仅作新建明细时的预填、可逐行改写——粒度比"按分类一刀切 markup"更细。用户 2026-09-10 补充拍板（"管理费率"为 markup 更平易近人的命名）。
- **业务单（算）→ 对账单（归属）→ 汇总分摊（引用）**，费用数字只在最上游出现一次：
  1. 派车单算出 Stage A 国内拖车 → 生成 **物流对账单**（买价）；
  2. 出口装箱单 → 生成 **海运对账单**（买价，批核锁定，Stage B+C）；
  3. **费用汇总与分摊**：只引用上述两对账单的明细行 + 按送货单权重分摊，**自身不存任何费用数字、不重算、不重复**。
- 故"费用汇总会重复统计派车单费用"不成立——派车单费用仅存在于物流对账单，费用汇总只是读取。

### 12.1 三张成本单据与阶段（按运输条款过滤）
| 阶段 | 成本单据 | 怎么来 | 运输条款可见性 |
|---|---|---|---|
| Stage A 国内拖车+报关 | 物流对账单（⑭ 派车单算） | 公里×运价 算出来 | 全部（FOB/CIF/CFR/DAP） |
| Stage B 海运费+起运港 | 海运对账单（⑯，选出口装箱单生成+批核） | 查成本费率表(买价) 生成 | CIF/CFR/DAP（FOB 不计入，买方付） |
| Stage C 目的港+到仓拖车 | 海运对账单（同屏，DAP 才勾） | 查成本费率表(买价) 生成 | DAP（含目的港到客户仓拖车） |

- 运输条款（出货通知单表头）决定哪些阶段计入"我们的成本"：**FOB→仅 A；CIF/CFR→A+B；DAP→A+B+C**。
- 海运费/起运港/目的港费用部分**必选**（自动勾）、部分**可选**（开单时勾选）；开单 → 批核 → 锁定，留痕防错。

### 12.2 海运对账单（⑯）生成 + 批核
- 选**出口装箱单号** → 带出柜型/起运港/目的港/运输条款 → 查成本费率表自动列出 **海运费 + 起运港费 + 目的港费**。
- 费用分必选/可选；开单人勾选适用项并提交 → 批核人**批核锁定**（状态机：草稿 → 待批核 → 已批核）。
- 生成的是**买价**（我们付货代的成本），不叠加 markup（markup 仅报价单卖价侧）。
- 批核后明细即作为 ⑮ Stage B+C 的成本来源。

### 12.3 数据来源（费用明细·实际发生，**引用不录入**）
| 来源单 | 阶段 | 费用 |
|---|---|---|
| 物流对账单 LB | A | 国内拖车费、出口报关费 |
| 海运对账单 SB | B | 海运费、THC、订舱费、封条费、BAF/CIC 等附加费 |
| 海运对账单 SB | C | 目的港费、目的港到仓拖车、关税（DAP） |

### 12.4 汇总与分摊引擎
1. 按「出货通知单号 / 柜号」引用两对账单明细 → 该票总费用（CNY 折算）+ 各分类小计（按运输条款过滤阶段）。
2. **分摊基准 = 送货单明细行**（客户 × 料号 × PO × 体积/重量）。
3. 分摊权重（默认 **体积优先**，可切换）：体积 / 重量 / 货值。
4. 每行分摊运费 = 总费用 × 该行权重占比。

### 12.5 输出视图
- 按出货通知单：总费用 + 分类小计；
- 按客户：各委托方承担运费（分账/开票）；
- 按客户PO：每 PO 运费（对账）；
- 按料号：每料号单位运费（元/CBM、元/吨）→ 回灌报价校准；
- 估实差异：报价单预估（卖价）vs 实际结算（买价归集），差异为正 = 实际超估，用于校准。

### 12.6 与既有设计的关系
- 取代并扩展 P1-R8（原仅"按客户体积/重量分摊柜租/海运费"）；现覆盖全分类、到 PO/料号粒度。
- 依赖「出货通知单/柜」父实体（R1）与「送货单」明细粒度（R3）。
- 成本费率表即原「运费明细表」重定位为买价；报价单 = 买价 ×(1 + 各项目自身管理费率)（逐行，默认管理费率见④、可改写；markup 平易近人化命名为"管理费率"，2026-09-10 拍板）。
- 分摊权重 = **体积优先**（用户 2026-09-10 拍板）。

## 13. 开放问题（Open Questions）

| # | 问题 | 需谁回答 | 是否阻塞 |
|---|---|---|---|
| Q1 | 一柜多客户时，主装箱单是否对外也展示客户拆分？还是纯内部？ | 业务/报关 | 非阻塞 |
| Q2 | 封条号在装柜前还是封柜后生成？影响装箱单必填时机 | 操作 | 非阻塞 |
| Q3 | 运费分摊权重：体积优先还是重量优先，或二者取大？ | 财务 | **已定：体积优先**（2026-09-10 拍板）；可切换重量/货值 |

---

## 14. TAPD 子项拆分建议

| 子项 | 归属 | 关键内容 |
|---|---|---|
| 出货通知单/发运单 | 父 | 字段、状态机、作为三单锚点 |
| 出口装箱单 | 本模块 | 字段(含封条号/柜号/合并维度)、软关联、合并规则、PO子明细、主单/分单 |
| 送货单 | 本模块 | 料号×PO 明细、关联出货通知单、实发回写 |
| 派车单联动 | 关联 | 选装箱单号带出承运数据（已设计） |
| 费用汇总与分摊 | 本模块 | 引用物流对账单+海运对账单(不重算) + 按出货通知单汇总(运输条款过滤阶段) + 经送货单分摊(体积优先) + 估实差异（§12，P1） |
| 海运对账单 | 本模块 | 选出口装箱单→成本费率表买价生成海运费+起运港+目的港，必选/可选+开单批核锁定（⑯，Stage B+C，买价侧） |
| 物流对账单 | 关联 | 派车单算 Stage A 拖车费生成（⑭，买价侧） |
| 成本费率表(买价) | 基础 | 原运费明细表重定位为买价；每行自带管理费率；报价单=买价×(1+各项目管理费率)，默认管理费率见④（可逐行改写） |
| 运费分摊 | 依赖 | 由「费用汇总与分摊」实现，按客户/PO/料号分摊（R8→§12） |

---

*设计依据：沿用项目"数据与展示分离、单一数据源"原则；父实体+软关联+多客户粒度已与业务确认（2026-09-04）；「运输条款」(合同级) 与「成交方式」(报关级贸易术语) 的层级区分补充于 2026-09-10；「费用汇总与分摊（实际结算）」模块 + 海运对账单(⑯) 补充于 2026-09-10，确立三层模型（成本费率表买价→物流对账单/海运对账单→费用汇总引用分摊，防重复）；成本费率表即原运费明细表重定位为买价，报价单=买价×(1+各项目自身管理费率)（管理费率逐行、2026-09-10 补充拍板，"管理费率"为 markup 平易近人化命名；④默认管理费率按分类预填、可逐行改写）；分摊权重=体积优先。*
