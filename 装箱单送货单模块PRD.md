# PRD｜出口装箱单 & 送货单 子模块（运费项目明细设定）

> 文档版本：v0.1（评审稿）　|　作者：产品　|　日期：2026-09-04　|　归属模块：运费项目明细设定
> 适用范围：海运出口业务，装箱单 / 送货单 / 派车单 三单联动设计

---

## 1. 问题陈述（Problem Statement）

在把"运费项目明细设定"模块拆子项时，出口装箱单与送货单的关系出现两个反复踩坑的设计疑问：

- **次序不清**：两份单都必不可少，但业务上谁先谁后不固定（工厂自送常"送货单先→装柜→装箱单"；先预报报关则"装箱单先→拉货再送货单"；外包货代则工厂只出送货单）。若强设先后依赖，部分客户流程直接走不通。
- **粒度冲突**：送货单需按"料号 × 客户PO"分行（客户要按 PO 追溯），装箱单却需按"料号"合并（报关/装柜只看物理包装）。同时一个柜常拼多家委托方客户的货，原设计缺"客户"维度，导致运费分摊、分账无法落地。

代价：不解决则子项拆分后各单数据对不齐、运费算不清、报关装箱单与内部账目脱节，上线即返工。

---

## 2. 目标（Goals）

| # | 目标 | 如何衡量成功 |
|---|---|---|
| G1 | 三单（出货委托/送货单/装箱单/派车单）数据可对齐、可互查 | 同一出货委托号下，任意两单能自动匹配关联 |
| G2 | 装箱单支持多合并维度，同时满足报关与内部分账 | 同一数据源可一键生成"按料号主单"与"按客户分单" |
| G3 | 支持一柜多委托方客户，运费可按客户体积/重量分摊 | 一个柜的运费能拆分到各客户且无遗漏/重复 |
| G4 | 不强制单序，适应 A/B/C 三类业务流 | 三种流程均可完整走通，无需改数据结构 |

---

## 3. 非目标（Non-Goals）

| # | 不做什么 | 原因 |
|---|---|---|
| NG1 | 不做越南进口报关单对接 | 已单列二期，避免领导误解（见前期结论） |
| NG2 | 不内置报关申报/舱单直传海关 | 属报关系统职责，本模块只出单 |
| NG3 | 不做多语言箱单（仅中文+必要英文品名） | 当前客户群不需要，后续可加 |
| NG4 | 不绑定特定 ERP 的库存接口 | 保持数据源独立，先接本系统销售订单/委托单 |

---

## 4. 用户故事（User Stories）

- 作为**操作员**，我希望在创建装箱单/送货单时都能选择同一个"出货委托号"，以便三单自动关联、互相对账。
- 作为**操作员**，我希望装箱单能按"料号"合并出主单给报关、又能按"客户"出分单给财务，以便一套数据两处用。
- 作为**财务**，我希望一个柜的运费能按客户的体积/重量分摊到各委托方，以便向每家客户独立开票。
- 作为**跟单**，我希望送货单按"料号 × 客户PO"分行、装箱单保留 PO 子明细，以便客户按 PO 对账、分票报关。
- 作为**管理者**，我希望无论"先送货单后装箱单"还是"先装箱单后送货单"流程都能跑，以便适配不同客户习惯。

---

## 5. 需求（Requirements）

### P0 必须（MVP）

**R1｜父实体"出货委托/发运单" + 柜子实体**
- 出货委托（Shipment）为唯一锚点，承载：出货委托号(PK)、起运港、目的港、船名航次、预计开船日、状态。**创建者=发货人侧跟单/业务/单证岗（或从 ERP 销售订单自动生成）；创建节点=销售订单确认、安排发货之后，且早于三单与订舱/报关（见 §5.5）。**
- **柜（Container）为 Shipment 子实体**：柜号(PK)、封条号、箱数、毛重、体积、提单号；一委托可含多柜（一票多柜）。
- 送货单、装箱单、派车单 均含 `出货委托号(FK)` 关联父实体；装箱单另含 `柜号(FK)`。

**R2｜软关联、无强制先后**
- 装箱单"关联送货单号"字段**可选**，在"确认/封板"时填写；系统优先用共同 `出货委托号/柜号` 自动匹配（一个柜可对应多张送货单/装箱单）。
- 不得把任一单设为另一单的创建前置条件。

**R3｜单一数据源粒度 = 客户 × 料号 × PO × 柜**
- 送货单明细行、出货委托明细行以 `客户 × 料号 × PO × 柜` 为最细粒度（客户、柜均属明细维度，非仅表头）。
- 装箱单为该数据源的聚合视图（按柜/总箱单/按客户/按PO），不另存物理明细。

**R4｜装箱单合并维度（至少三种）**
- 支持"按柜（每柜一张，基准）"、"总箱单（跨柜汇总，报关/总账）"、"按客户（分单/分账）"。
- 合并键 = `料号`（同料号须箱规一致；箱规不同则 `料号+箱规`）。

**R5｜多客户（一柜多委托方）**
- 一个出货委托可挂多个委托方客户；主装箱单对外（GROUP BY 料号），分箱单×客户对内（GROUP BY 客户）。

### P1 应做（上线后快跟）

- **R6｜PO 子明细**：合并后的装箱单行可展开显示 `PO号 / 数量 / 箱数 / 占比`，满足追溯与分票报关。
- **R7｜合并维度扩展**：增加"按料号+PO""按PO"两种视图。
- **R8｜运费分摊**：由「费用汇总与分摊」模块（§12）统一实现；按出货委托归集全部海运费用后，经送货单明细（客户×料号×PO×体积/重量）分摊到各客户/PO/料号，默认权重=体积优先，可切换重量/货值。

### P2 未来（架构预留）

- **R9｜分票报关**：一个柜按客户/PO 拆多票报关时，装箱单自动对应多份报关资料。
- **R10｜LCL 主单/分单（house B/L）**：拼箱场景主单一个、分单多个。

---

## 5.5 出货委托（Shipment）：流程起点与创建规则

> 本模型的根实体。此前为解开"三单次序不清"把它作为共同锚点引入，但它本身从何而来必须讲清，否则下游单据的出货委托号成了无源之水。

**是什么**：出货委托（又称出货通知单 / 发运单 / 托单 / 订舱委托）是"**我要发这一票货**"的业务起点单据，代表一笔待发运的出口业务。下游装箱单、送货单、派车单、费用、报关全部挂在它下面。

**谁开**：发货人（卖方）侧的 **跟单 / 业务 / 单证岗**；或由 **ERP 销售出货模块**从已确认销售订单自动生成。

**何时开 / 哪个节点**：**销售订单确认、决定安排发货之后**，且 **早于** 装箱单、送货单、派车单、订舱（SO）、报关。它是整条出口流程的**最上游**，必须先存在。

```
销售订单（客户要买什么）
   ↓ 确认、安排发货
【出货委托 Shipment】→ 系统生成 出货委托号   ← ★ 本节点创建，流程起点
   ↓ 之后才能开
   ├─ 出口装箱单（预报/实际）   出货委托号(FK)
   ├─ 送货单（仓库备货）       出货委托号(FK)
   ├─ 派车单（拖车拉柜）       出货委托号(FK)
   └─ 费用汇总与分摊           出货委托号(FK)
```

**与销售订单的关系**：销售订单 = 客户买了什么；出货委托 = 我要发这票货。一张销售订单可拆多票出货委托（分批发运），多张销售订单也可合并成一票出货委托（拼货 / consolidation）。

**创建方式**：手动新建，或从销售订单"生成出货委托"；创建时系统自动生成 `出货委托号(PK)`，并填起运港、目的港、预计开船日、客户、运输条款、成交方式（默认=运输条款）、计划柜数、状态（草稿）。

**关键约束**：下游单据的 `出货委托号` 是**引用（FK）已存在的 Shipment**，本模块不生成该号；未建出货委托则下游单据开不了（出货委托号必填）。

---

## 6. 数据模型

### 6.1 实体关系

```
出货委托/发运单 (Shipment)            ← 父实体，唯一锚点；【流程起点，最先创建，见 §5.5】（可含多个柜）
 ├─ 柜 (Container)     出货委托号(FK)   ← 柜号/封条号/箱数/体积/重量/提单号，装箱单承载单元
 ├─ 送货单 (DO)        出货委托号(FK)   ← 仓库放行·拖车依据，明细 客户×料号×PO×柜
 ├─ 装箱单 (PL)        柜号(FK)         ← 每柜一张；报关·装船；总箱单为其聚合视图
 └─ 派车单 (Dispatch)  出货委托号(FK)   ← 关联柜/装箱单号带出承运体积/重量（已有设计）
```

### 6.2 粒度与视图映射

| 层级 | 粒度 | 用途 |
|---|---|---|
| 单一数据源 | 客户 × 料号 × PO × 柜 | 送货单/出货委托明细（最细，每箱归属某柜） |
| 装箱单·按柜 | GROUP BY 料号（限定单柜） | 每柜一张，封条核验/收货人拆柜（对外基准） |
| 装箱单·总箱单 | GROUP BY 料号（跨柜汇总） | 整票报关/总账（对外汇总） |
| 装箱单·分单 | GROUP BY 客户 → 料号 | 分账 / 运费分摊（对内） |
| 装箱单·追溯 | GROUP BY 料号+PO 或 PO | 内部核对（P1） |

---

### 6.3 多集装箱场景（一票多柜）

- 同一出货委托下挂多个 柜（Container 子实体各一条，如 3 个柜）。
- 基准做法：**每柜开一张装箱单**（柜号+封条号表头），满足海关验封、收货人逐柜拆货。
- 系统一键生成**总箱单**（跨全部柜 GROUP BY 料号 汇总），用于整票报关与总账。
- 业务变体由"合并维度"开关覆盖，无需改结构：
  - 3 柜同属一票、客户只收一份 → 仅出总箱单；
  - 3 柜分属不同提单/收货人 → 各自独立装箱单、无总箱单。

### 6.4 运输条款 vs 成交方式（报关级贸易术语）

| 维度 | 主表「运输条款」 | 装箱单「成交方式」 |
|---|---|---|
| 层级 | 合同级（出货委托/Shipment 表头） | 报关级（装箱单**表头/分单**，整单一个） |
| 来源 | 销售合同 / 订单 | 默认 = 主表运输条款；多客户分单可按客户不同 |
| 用途 | 决定海运费由买方还是卖方承担（运费估价开关） | 海关申报、审价、统计 |
| 典型值 | FOB、CIF、CFR、EXW 等 | FOB、CFR(C&F)、CIF 为主（见 §7.1） |
| 是否可一单多值 | 否，整票一个 | 主单统一；仅按客户出分单时各分单可不同 |

> 实务中**同一装箱单（同一票货）成交方式通常统一**，明细行直接继承表头即可，不必逐料号填。只有"一柜拼多家客户、各客户独立合同"时才按客户出分单、各分单成交方式不同。运费估价仍看主表运输条款。

## 7. 字段清单（草案）

### 7.0 柜（Container，Shipment 子实体）
| 字段 | 说明 | 备注 |
|---|---|---|
| 柜号 | PK（出货委托内唯一） | 自动/手填 |
| 封条号 | 物理封条 | 装柜后生成 |
| 箱数 / 毛重 / 体积 | 该柜汇总 | 自动算 |
| 提单号 | 对应 B/L | 一柜一提单或一票多柜 |

### 7.1 出口装箱单（PL）

| 字段 | 说明 | 备注 |
|---|---|---|
| 装箱单号 | PK | 自动生成 |
| 出货委托号 | FK → Shipment | 软关联键 |
| 柜号 | FK → Container | 引用柜子实体（柜号/封条号由其承载） |
| 运输条款 | 引用 Shipment（只读） | 合同级，表头打印，与发票/提单一致 |
| 成交方式 | 默认 = 运输条款（只读）；多客户分单可改 | 报关级，整单一个；用于海关申报 |
| 合并维度 | 按柜 / 总箱单 / 按客户 / 按料号+PO / 按PO | 生成时选择 |
| 单类 | 按柜箱单 / 总箱单 / 分单(客户A…) | 多柜出按柜箱单+总箱单；多客户出分单（各分单成交方式可不同） |
| 总件数 / 总毛重 / 总体积 | 汇总 | 自动算 |
| 明细行 | 料号、品名、箱数、毛重、净重、体积、PO子明细 | 合并后行可展开 PO；成交方式继承表头 |
| 报关字段组 | HS编码、单价、币种、原产国、境内货源地 | 海关申报用；成交方式在表头，不逐行填 |

### 7.2 送货单（DO）

| 字段 | 说明 | 备注 |
|---|---|---|
| 送货单号 | PK | 自动生成 |
| 出货委托号 | FK → Shipment | 软关联键 |
| 发货厂址 / 起运港 | 物流执行信息 | 必填 |
| 委托方客户 | 明细行归属 | 一行一客户 |
| 明细行 | 客户 × 料号 × PO、应发/实发数量、箱数、毛重、体积 | 同料号按 PO 分行 |

---

## 8. 合并维度规则（核心逻辑）

1. 取该 `出货委托号` 下全部 `客户 × 料号 × PO × 柜` 明细。
2. **按柜（基准）**：限定单柜号，组内按料号合并，生成"每柜一张"装箱单；柜号+封条号为表头，供海关验封与收货人拆柜。
3. **总箱单**：跨全部柜号 GROUP BY 料号 汇总，输出整票总箱单（报关/总账用）。
4. **按客户**：先按委托方客户分组，组内再按料号合并，输出"分箱单×客户"（分账/运费分摊）。
5. **按料号+PO / 按PO**：保留 PO 级行（追溯/分票用）。
6. 同料号箱规不同 → 不参与合并，各自成行并标注箱规。

---

## 9. 状态机（建议）

```
草稿 ──确认/封板──▶ 已确认 ──报关──▶ 已报关 ──装船──▶ 已发运
  │                  │
  └──── 作废 ────────┘
```
- "关联送货单号"在**已确认**节点填写（软匹配）。
- 已确认后不允许改合并维度主档，需废版重出。

---

## 10. 验收标准（节选 AC）

- [ ] Given 一个出货委托号，When 创建装箱单并选"按料号"，Then 同料号多 PO 合并为一行且 PO 子明细可见。
- [ ] Given 一柜含客户 A、B、C 的货，When 选"按客户"生成，Then 输出三张分箱单且各客户体积/重量合计 = 主单总计。
- [ ] Given 先建送货单（未建装箱单），When 随后建装箱单并填同一出货委托号，Then 系统自动关联，无强制先后拦截。
- [ ] Given 同料号但箱规不同，When 合并，Then 不强行合并，分别成行。
- [ ] Given 已确认装箱单，When 改合并维度，Then 被拒，须作废重出。

---

## 11. 成功指标（建议）

| 指标 | 目标 | 评估时点 |
|---|---|---|
| 三单关联成功率 | ≥98%（同委托号可互查） | 上线 1 月 |
| 装箱单生成耗时 | 较手工 < 5 分钟/柜 | 上线 1 月 |
| 运费分摊差错率 | 0（按体积/重量自动分摊） | 上线 1 季 |
| 多客户柜占比 | 可统计（基线先采集） | 持续 |

---

## 12. 费用汇总与分摊（实际结算）模块

> 与「⑥ 报价单生成」是两套：报价单 = 事前估价（按费率表聚合）；本模块 = 事后实际结算（按派车单/账单归集，再经送货单分摊到订单/客户PO/料号）。报价单原型已明确"不含报关费/关税/目的港费"，本模块补齐这些实际发生项。

### 12.1 业务价值
- 报价单只算预估；本模块归集实际发生的全部海运费用（国内拖车/起运港/海运费/目的港/报关/关税），并落到每个订单 / 客户PO / 料号，支撑**客户对账、分账、利润核算**，差异数据回灌**费率校准**。

### 12.2 数据来源（费用明细·实际发生）
| 来源单 | 费用 | 说明 |
|---|---|---|
| 派车单（⑭） | 国内拖车费（厂址→起运港） | 自动从派车单带出/回写 |
| 供应商/货代账单 | 起运港费（THC/订舱/封条等） | 录入或对接账单 |
| 船公司账单 | 海运费 | 录入或对接账单 |
| 货代/目的港代理账单 | 目的港费用 | 录入 |
| 报关行账单 | 报关费 | 录入 |
| 目的国税单 | 关税 | 录入（CIF/CFR 卖方承担时） |

费用明细表字段：出货委托号/柜号(FK) · 费用分类 · 费用项目 · 金额 · 币种 · 来源单 · 发生日期。

### 12.3 汇总与分摊引擎
1. 按「出货委托号 / 柜号」汇总 → 该票总费用（CNY 折算）+ 各费用分类小计。
2. **分摊基准 = 送货单明细行**（客户 × 料号 × PO × 体积/重量）：费用归集在委托/柜级，分摊比例由送货单提供（呼应 R3 粒度）。
3. 分摊权重（默认 **体积优先**，可切换）：
   - 体积优先：按 `Σ(件数×单件体积)` 占比；
   - 重量优先：按 `Σ(件数×单件毛重)` 占比；
   - 货值优先：按 `Σ(数量×单价)` 占比（单价取装箱单）。
4. 每行分摊运费 = 总费用 × 该行权重占比。

### 12.4 输出视图
- 按出货委托：总费用 + 分类小计；
- 按客户：各委托方承担运费（分账/开票）；
- 按客户PO：每 PO 运费（对账）；
- 按料号：每料号单位运费（元/CBM、元/吨）→ 回灌报价校准；
- 估实差异：报价单预估（费率表）vs 实际结算，差异为正=实际超估，用于校准。

### 12.5 与既有设计的关系
- 取代并扩展 P1-R8（原仅"按客户体积/重量分摊柜租/海运费"）；现覆盖全分类、到 PO/料号粒度。
- 依赖「出货委托/柜」父实体（R1）与「送货单」明细粒度（R3）。
- 分摊权重 Q3 已定：**体积优先**（用户 2026-09-10 拍板）。

## 13. 开放问题（Open Questions）

| # | 问题 | 需谁回答 | 是否阻塞 |
|---|---|---|---|
| Q1 | 一柜多客户时，主装箱单是否对外也展示客户拆分？还是纯内部？ | 业务/报关 | 非阻塞 |
| Q2 | 封条号在装柜前还是封柜后生成？影响装箱单必填时机 | 操作 | 非阻塞 |
| Q3 | 运费分摊权重：体积优先还是重量优先，或二者取大？ | 财务 | 阻塞 R8 |

---

## 14. TAPD 子项拆分建议

| 子项 | 归属 | 关键内容 |
|---|---|---|
| 出货委托/发运单 | 父 | 字段、状态机、作为三单锚点 |
| 出口装箱单 | 本模块 | 字段(含封条号/柜号/合并维度)、软关联、合并规则、PO子明细、主单/分单 |
| 送货单 | 本模块 | 料号×PO 明细、关联出货委托、实发回写 |
| 派车单联动 | 关联 | 选装箱单号带出承运数据（已设计） |
| 费用汇总与分摊 | 本模块 | 费用明细·实际发生 + 按出货委托汇总 + 经送货单分摊(体积优先) + 估实差异（§12，P1） |
| 运费分摊 | 依赖 | 由「费用汇总与分摊」实现，按客户/PO/料号分摊（R8→§12） |

---

*设计依据：沿用项目"数据与展示分离、单一数据源"原则；父实体+软关联+多客户粒度已与业务确认（2026-09-04）；「运输条款」(合同级) 与「成交方式」(报关级贸易术语) 的层级区分补充于 2026-09-10；「费用汇总与分摊（实际结算）」模块补充于 2026-09-10（取代并扩展 R8，分摊权重=体积优先，与报价单"事前估价"构成估结两套）。*
